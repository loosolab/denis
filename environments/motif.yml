name: motif

channels:
  - bioconda
  - conda-forge

# unless not further specified are all versions fixed to speed up build time

dependencies:
  - python>=3.7.16
  - tobias>=0.13.3
  - scipy>=1.7.3
  - pandas>=1.3.5
  - kneed>=0.7.0
  - biopython>=1.79
  - seaborn>=0.12.2
  - matplotlib>=3.5.3
  - lxml>=4.8
  - numpy>=1.18 # version fixes issue
  - meme>=5.3.0 # version fixes issue
  - gimmemotifs>=0.18 # version fixes issue
  - sklearn-contrib-lightning>=0.6.0 # needed by gimmemotifs
  - pyarrow>=9.0.0 # needed by gimmemotifs
  - bedtools>=2.30
  - lmfit>=1.1.0
  - matplotlib-venn>=0.11.7
  - pybedtools>=0.9
  - uropa>=4.0.2
